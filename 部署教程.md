# GitHub Pages 部署教程

## 📦 部署步骤

### 第一步: 创建 GitHub 仓库

1. 访问 [GitHub](https://github.com/)
2. 点击右上角 "+" → "New repository"
3. 填写仓库信息:
   - **Repository name**: `ai-content-assistant` (或你喜欢的名字)
   - **Description**: 人机协同内容助手
   - **Public**: 选择 Public (公开)
   - ✅ 勾选 "Add a README file"
4. 点击 "Create repository"

---

### 第二步: 上传文件

#### 方法一: 网页上传 (推荐,最简单)

1. 进入你创建的仓库页面
2. 点击 "Add file" → "Upload files"
3. 将 `deploy` 文件夹中的所有文件拖拽到页面:
   - `index.html` (主文件)
   - `README.md`
4. 在底部填写提交信息: "初始化部署"
5. 点击 "Commit changes"

#### 方法二: 使用 Git 命令行

```bash
# 进入 deploy 目录
cd d:\GZHWZ\deploy

# 初始化 Git
git init

# 添加远程仓库 (替换为你的仓库地址)
git remote add origin https://github.com/你的用户名/ai-content-assistant.git

# 添加文件
git add .

# 提交
git commit -m "初始化部署"

# 推送到 GitHub
git branch -M main
git push -u origin main
```

---

### 第三步: 开启 GitHub Pages

1. 在仓库页面,点击 "Settings" (设置)
2. 在左侧菜单找到 "Pages"
3. 在 "Source" 下:
   - Branch: 选择 `main`
   - Folder: 选择 `/ (root)`
4. 点击 "Save"
5. 等待 1-2 分钟部署完成

---

### 第四步: 访问你的网站

部署成功后,页面顶部会显示:

```
✅ Your site is published at https://你的用户名.github.io/仓库名/
```

点击链接即可访问! 🎉

---

## 🌐 访问地址

你的网站地址将是:
```
https://你的用户名.github.io/ai-content-assistant/
```

例如:
- 用户名: `zhangsan`
- 仓库名: `ai-content-assistant`
- 访问地址: `https://zhangsan.github.io/ai-content-assistant/`

---

## 🔄 更新网站

修改文件后:

### 网页上传更新:
1. 进入仓库
2. 点击要修改的文件
3. 点击编辑按钮 (铅笔图标)
4. 修改后点击 "Commit changes"
5. 等待 1-2 分钟自动部署

### Git 命令行更新:
```bash
cd d:\GZHWZ\deploy
git add .
git commit -m "更新内容"
git push
```

---

## 🎯 自定义域名 (可选)

如果你有自己的域名:

1. 在 GitHub Pages 设置页面
2. 在 "Custom domain" 填入你的域名
3. 点击 "Save"
4. 在域名服务商添加 CNAME 记录:
   - 类型: CNAME
   - 主机记录: www (或其他)
   - 记录值: `你的用户名.github.io`

---

## ⚠️ 注意事项

### API Key 安全

由于是纯前端应用,API Key 会暴露在代码中。建议:

1. **使用限额较低的 API Key**
   - 设置使用限额
   - 定期更换

2. **添加域名限制** (如果 API 服务支持)
   - 只允许你的域名调用

3. **监控使用情况**
   - 定期检查 API 使用量
   - 发现异常及时更换

### 更安全的方案

如需更高安全性,可以:
1. 创建后端 API 代理
2. 使用 Vercel/Netlify 的 Serverless 函数
3. 隐藏真实 API Key

---

## 🐛 常见问题

### Q: 页面显示 404
A: 
- 检查 GitHub Pages 是否已开启
- 确认文件名是 `index.html`
- 等待 1-2 分钟部署完成

### Q: 样式显示不正常
A:
- 清除浏览器缓存
- 检查文件是否完整上传

### Q: API 调用失败
A:
- 检查 API Key 是否正确
- 确认网络连接正常
- 查看浏览器控制台错误信息 (F12)

### Q: 如何删除网站
A:
1. 进入仓库设置
2. 拉到最底部 "Danger Zone"
3. 点击 "Delete this repository"

---

## 📞 需要帮助?

部署过程中遇到问题,可以:
1. 查看 [GitHub Pages 官方文档](https://docs.github.com/cn/pages)
2. 检查仓库的 "Actions" 标签查看部署日志
3. 在浏览器按 F12 查看控制台错误

---

**祝你部署顺利! 🎉**
